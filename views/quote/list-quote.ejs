<div class="min-h-screen px-4 py-6 sm:px-8 max-w-4xl mx-auto">
      <h1 class="text-3xl font-bold mb-6 text-center">Quote Collection</h1>

      <div class="grid gap-6">
        <% quotes.forEach(quote => { %>
          <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition">
            <p class="text-lg italic text-gray-700 mb-2">"<%= quote.content %>"</p>
            <p class="text-sm text-gray-500 text-right">— <%= quote.author || 'Anonymous' %></p>
          </div>
        <% }) %>
      </div>
    </div>
